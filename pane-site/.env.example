# ─────────────────────────────────────────────
#  pane — environment configuration
#  Copy this file to .env and fill in your keys
#  Never commit .env to version control
# ─────────────────────────────────────────────

# ── EMAIL ALIASING ──────────────────────────
# Addy.io (primary) — get key at addy.io/settings/api
ADDY_API_KEY=

# SimpleLogin (alternative) — get key at app.simplelogin.io/dashboard/api-key
# Leave blank if using Addy.io
SIMPLELOGIN_API_KEY=

# Which service to use: "addy" | "simplelogin"
EMAIL_PROVIDER=addy

# Your alias domain (optional — uses provider default if blank)
# Example: pane.yourdomain.com
ALIAS_DOMAIN=

# ── VIRTUAL CARDS ───────────────────────────
# Privacy.com (US only) — get key at app.privacy.com/developer
PRIVACY_API_KEY=

# Default card spend limit in cents (0 = no funds until manually loaded)
CARD_DEFAULT_LIMIT_CENTS=0

# ── NETWORK / VPN ───────────────────────────
# Mullvad account number (no email required) — mullvad.net/en/account
# Used to assign per-context exit nodes via Mullvad API
MULLVAD_ACCOUNT=

# ProtonVPN credentials (free tier fallback — no API, used for manual setup docs)
# PROTON_USER=
# PROTON_PASS=

# ── PHONE / SMS ─────────────────────────────
# JMP.chat — jmp.chat (requires XMPP account)
JMP_API_KEY=
JMP_XMPP_JID=

# ── IDENTITY PARAMETERS ─────────────────────
# Region for generated identities
# Options: US-West | US-East | US-Midwest | US-South | UK | CA | AU
PANE_BASE_REGION=US-West

# Name style for generated identities
# Options: generic-anglo | hispanic | east-asian | south-asian | mixed
PANE_NAME_STYLE=generic-anglo

# Always override these fields with fake values in signups
# Comma-separated list of: dob,phone,address,gender,birthday
PANE_FAKE_FIELDS=dob,phone,address

# ── BROWSER AUTOMATION ──────────────────────
# Headless mode for Playwright (true = invisible, false = watch it work)
PLAYWRIGHT_HEADLESS=true

# Slow down automation in ms — useful for debugging stubborn signup flows
PLAYWRIGHT_SLOW_MO=0

# ── CAPTCHA ─────────────────────────────────
# 2captcha API key — 2captcha.com (LATER tier)
TWOCAPTCHA_API_KEY=

# ── STORAGE ─────────────────────────────────
# Where pane stores context data locally (SQLite)
# Default: ~/.pane/contexts.db
PANE_DB_PATH=

# ── SERVER ──────────────────────────────────
# MCP server transport: "stdio" (Claude Desktop) | "http" (custom clients)
MCP_TRANSPORT=stdio

# Port if using HTTP transport
MCP_PORT=3141

# Log level: "silent" | "error" | "warn" | "info" | "debug"
LOG_LEVEL=info
